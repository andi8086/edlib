;-------------------------------------------------------------------------------
; Purpose:      Sets the OVERSCAN color, useful for rastertime checks.
;-------------------------------------------------------------------------------
; Destroys:     AL, BL, DX.
;-------------------------------------------------------------------------------
; Requirements: None.
;-------------------------------------------------------------------------------
; Parameters:   BL = Overscan color.
;-------------------------------------------------------------------------------
; Returns:      None.
;-------------------------------------------------------------------------------
; Comments:     Nop's are possibly required on heavy machines.
;-------------------------------------------------------------------------------
Raster          proc
                mov     dx,3dah
                in      al,dx
                nop
                mov     dx,3c0h
                in      al,dx
                mov     al,31h
                nop
                out     dx,al
                mov     al,bl
                nop
                out     dx,al
                ret
                endp
